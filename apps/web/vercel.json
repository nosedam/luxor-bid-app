{
  "buildCommand": "cd ../.. && pnpm --filter @workspace/db exec prisma generate --schema=./prisma/schema.prisma && pnpm turbo build --filter=web",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
